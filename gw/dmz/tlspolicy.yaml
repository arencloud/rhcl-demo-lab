apiVersion: kuadrant.io/v1
kind: TLSPolicy
metadata:
  name: rhcl-demo-lab-dmz-tls-policy
  namespace: app-global-dmz
spec:
  targetRef:
    group: gateway.networking.k8s.io
    kind: Gateway
    name: rhcl-demo-lab-dmz
  issuerRef:
    name: webapps
    kind: ClusterIssuer