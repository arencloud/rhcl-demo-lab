kind: Service
apiVersion: v1
metadata:
  name: order
  namespace: orders
  labels:
    app: order
    service: order
    version: v1
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  clusterIP: None